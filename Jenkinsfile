pipeline {
    agent any
    stages {
        stage ("pipeline node") {
                stage('instalar dependencias') {
                    steps {
                        sh 'npm install'
                    }
                }
        }
    }
}